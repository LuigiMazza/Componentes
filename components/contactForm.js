const root=document.getElementById('contact-form-root');if(root){root.innerHTML=`<form id='contact-form' class='contact-form'><label>Nombre<input type='text' name='name' required></label><label>Email<input type='email' name='email' required></label><label>Mensaje<textarea name='message' rows='5' required></textarea></label><button type='submit'>Enviar ✈️</button><p id='form-status'></p></form>`;const form=document.getElementById('contact-form');const status=document.getElementById('form-status');form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='Enviando...';const data=new FormData(form);const message=`Nombre: ${data.get('name')} Email: ${data.get('email')} Mensaje: ${data.get('message')}`;try{await fetch('https://apx.school/api/utils/email-to-student',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({to:'tu-email-de-estudiante@gmail.com',message})});status.textContent='¡Mensaje enviado!';form.reset()}catch{status.textContent='Error, intenta de nuevo'}})}